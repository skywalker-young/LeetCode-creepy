arr=[i for i in range(1,n+1,1)]
print(arr)

print('%',4%6)

ind=0
for i in range(9):
    print(ind)
    print(len(arr))
    ind=(ind+m)%len(arr)
    ind=ind-1  #2,4...

    print('death',arr[ind])
    arr.pop(ind)
    #del (arr[ind])
    if ind==-1:#last
        ind=0
print('live',arr[0])
